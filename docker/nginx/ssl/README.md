# TLS certificates

Place the certificate and private key configured by `SSL_CERT_FILE` and
`SSL_KEY_FILE` in this directory before setting `ENABLE_SSL=true`.

Do not commit certificates or private keys. The repository ignores every file
in this directory except this README.
